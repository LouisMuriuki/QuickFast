const recievePaymentRequest=(req: { body: { ownerid: any; packageId: any } },res: any)=>{
    const {ownerid,packageId}=req.body
     console.log(req.body)
    try {
        
    } catch (error) {
        
    }
}
export {recievePaymentRequest}